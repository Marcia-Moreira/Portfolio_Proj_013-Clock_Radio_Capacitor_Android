{
  "dependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/cli": "^7.2.0",
    "@capacitor/core": "^7.2.0"
  },
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "copiar": "powershell -ExecutionPolicy Bypass -File copiar_para_www.ps1",
    "deploy": "npm run build && npm run copiar && npx cap sync"
  }
}
